import { getProject } from "~/createWcp";

describe("get extra attributes", () => {
    it("should get a list of new attributes with definitions to add", async () => {
        const project = await getProject();

        const bb = 123;

    });
});
